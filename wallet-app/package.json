{
  "name": "quicktap-wallet-app",
  "private": true,
  "version": "1.0.0",
  "description": "Carcasa Android de QuickTap Wallet: abre el sitio en vivo en /wallet.",
  "scripts": {
    "sync": "cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/push-notifications": "^8.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
