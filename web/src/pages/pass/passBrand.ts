/**
 * Marca de QuickTap Pass.
 *
 * El logo definitivo está en camino: cuando llegue, se cambia esta única ruta y se actualiza
 * todo el portal. Por eso vive acá y no repetido en cada pantalla.
 */
export const PASS_LOGO_URL = '/logo/logo-central.png';
/**
 * Marca propia de Pass ("pass))" en blanco con las ondas azules), ya recortada al arte —
 * sin márgenes transparentes, así que un `h-*` da exactamente ese alto en pantalla.
 * Es blanca: solo va sobre fondo oscuro (la portada y el panel del cliente).
 */
export const PASS_WORDMARK_URL = '/logo/pass.png';
export const PASS_NAME = 'QuickTap Pass';
