import { QuoteManager } from '@/components/admin/QuoteManager';

/** Ajustes → Cotizaciones (restaurante): ver QuoteManager, compartido con Local Comercial. */
export default function QuotesPage() {
  return <QuoteManager />;
}
