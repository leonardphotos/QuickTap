/**
 * Marca de QuickTap Wallet.
 *
 * Vive acá y no repetida en cada pantalla: cuando cambie el logo se toca una sola ruta y se
 * actualiza todo el portal.
 */
export const WALLET_LOGO_URL = '/logo/logo-central.png';
/**
 * Marca propia de Wallet ("wallet))" en blanco con las ondas azules), ya recortada al arte —
 * sin márgenes transparentes, así que un `h-*` da exactamente ese alto en pantalla.
 * Es blanca: solo va sobre fondo oscuro (la portada y el panel del cliente).
 */
export const WALLET_WORDMARK_URL = '/logo/wallet.png';
export const WALLET_NAME = 'QuickTap Wallet';
