node_modules/
dist/
.env
.env.local
*.log
coverage/
.DS_Store
prisma/*.db
prisma/migrations/dev.db*
uploads/

# Salida del grafo de conocimiento (graphify): 10 MB de artefactos regenerables.
graphify-out/

# Caché incremental de TypeScript: cambia en cada compilación, no aporta nada al repo.
tsconfig.tsbuildinfo

# App Android del Wallet: dependencias y artefactos de build (la config de Firebase no se versiona)
wallet-app/node_modules/
wallet-app/android/app/build/
wallet-app/android/.gradle/
wallet-app/android/app/google-services.json

# Simulador de auditoría (herramienta local, no forma parte del producto)
.audit/
